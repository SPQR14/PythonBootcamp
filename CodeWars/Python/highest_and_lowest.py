def high_and_low(numeros):
    numeros2 = numeros.split(" ")
    mayor = int(numeros[0])
    menor = int(mnumeros[0])
    if numeros2.len == 1:
        return numeros
    else:
        for x in numeros.len:
            
    